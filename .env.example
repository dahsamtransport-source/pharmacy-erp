# Browser-safe configuration only. Never place a secret/service-role key here.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
# Existing AI route server secrets remain server-only; they are not used by this dashboard.
