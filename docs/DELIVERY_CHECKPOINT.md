# Delivery checkpoint — 2026-09-20

- Branch: `delivery/readiness-and-ledger`, based on `tooling/local-supabase` at `e3247b028a4eaed7b7b91f77e7ca7892f2213d47`. The isolated checkout is `/workspace/scratch/361a8bf1cdf1/pharmacy-erp-delivery`.
- User goal: truthful completion/remaining report and autonomous implementation for a single establishment. No production transactions, secrets in chat or unsupported claims of completion.
- Verified PR 7 and PR 8 remain open/unmerged; PR 8 is draft. Sites metadata: active version 15, `custom` access. The D1/R2 Site and this Supabase evaluation schema are separate. Neither Site nor production database was changed.
- Implemented: read-only `ym_api.account_ledger`, RLS/financial-role checks, scoped opening/movements/running/closing balances, source document and journal references, range/currency guards and explicit 1000-journal ceiling. Trial balance account names open a typed accessible dialog with retry and exact CSV export. No browser-provided approval or totals are trusted.
- Files: new migration `20260920184723_account_ledger_report.sql`, `src/lib/pharmacy/ledger.ts`, `src/features/pharmacy/AccountLedger.tsx`; contracts/API/report/CSS; six frontend and six SQL test cases; updated local README and readiness report.
- Validation completed: frontend 52 PASS; local tools 12 PASS; SQL/RLS on PGlite 60 PASS (total 124); ESLint PASS; TypeScript PASS; production build PASS. Focused UI follow-up after table-container CSS adjustment: 24 PASS. The 1000/1001-journal boundary retains every journal constraint and verifies rejection instead of truncated balances.
- Test repair: currency-mismatch fixture now reactivates a historical inactive account before attempting a new posting. The ledger still returns historical inactive-account balances. Bulk range fixtures retain triggers and deferred balance constraints; no constraints were bypassed. The large fixture initially produced a poor query plan with stale statistics; explicit test-only ANALYZE reduced the boundary test from tens of seconds/hanging combined runs to about 0.71 seconds, and the whole SQL suite passed in 4.42 seconds. Native production-size performance remains unverified.
- Blockers: no Docker/Podman/socket or Windows computer-control tool in this session; real Supabase Auth/PostgREST, native multi-connection concurrency, device/printer and restore tests remain unexecuted. No OCR/WhatsApp/Drive/Modern Soft activation was performed.
- Next: save reviewed change as a stacked draft PR above PR 8; then run the existing Docker/real Auth/concurrency checks on an accessible test environment. Base currency, fiscal year, tax policies, opening balances and insurance contracts still require owner decisions. See `docs/DELIVERY_READINESS_AR.md` for the module-by-module acceptance backlog.
- Preserve earlier original checkouts and `docs/engineering/PROJECT_STATE.yaml`. Do not reset the training database or apply the evaluation schema to a live database. Do not merge/deploy these branches without validating the migration path and intended environment.
